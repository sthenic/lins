extends: existence
message: "Remove 'Equation' or 'Equations'."
ignorecase: true
nonword: true
level: error
linter:
  - latex
latex:
  - name: eqref
    kind: control sequence
    before: (?i)(?<=(?<!^|\. )equation|(?<!^|\. )equations)~$
tokens:
  - 'eq:'
